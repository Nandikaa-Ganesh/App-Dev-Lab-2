function showDetails(itemId) {
    // Redirect to the detailed view page with the item ID
    window.location.href = `details.html?id=${itemId}`;
}

function contactOwner() {
    // Implement logic to handle contacting the owner (e.g., open a contact form)
    alert('Contacting owner. Implement your logic here.');
}
